$(document).ready(function() {
    // We define shorthands for bootstrap grid sizing elements, so that we can
    // change them in one place (here), rather than everywhere in the HTML file.
    $('.containerLeft').addClass('col-xs-12 col-md-8');
    $('.containerRight').addClass('hidden-xs hidden-sm col-md-2');
    $('.positionLeft').addClass('col-xs-8');
    $('.positionRight').addClass('col-xs-4');
});
